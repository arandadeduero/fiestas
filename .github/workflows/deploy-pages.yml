name: Deploy Pages

on:
  push:
    branches:
      - master
      - main
      - aranda
  schedule:
    - cron: "0 20 3-12 9 *"
  workflow_dispatch:
    inputs:
      daily_date:
        description: "Fecha a regenerar (opcional; se publican también las demás fechas)"
        required: false
        type: string

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  publication-window:
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.window.outputs.run }}
    steps:
      - name: Check 2026 publication window
        id: window
        shell: bash
        run: |
          if [ "$GITHUB_EVENT_NAME" != "schedule" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          current_date="$(date -u +%F)"
          if [[ "$current_date" > "2026-09-01" && "$current_date" < "2026-09-25" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

  build:
    needs: publication-window
    if: needs.publication-window.outputs.run == 'true' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Cache installed dependencies
        id: node-modules-cache
        uses: actions/cache@v6
        with:
          path: node_modules
          key: ${{ runner.os }}-node24-modules-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm ci --prefer-offline --no-audit --no-fund

      - name: Unit tests
        run: npm test

      - name: Build site
        run: npm run build

      - name: Generate daily popular Stories and posts
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        env:
          DAILY_POPULAR_DATE: ${{ inputs.daily_date }}
        run: |
          if [ -n "$DAILY_POPULAR_DATE" ]; then
            npm run social:daily -- --date "$DAILY_POPULAR_DATE" --all
          else
            npm run social:daily -- --all
          fi

      - name: Preserve generated daily posters on source pushes
        if: github.event_name == 'push'
        shell: bash
        run: |
          mkdir -p dist/daily-popular
          dates=(
            2026-09-04 2026-09-05 2026-09-06 2026-09-07 2026-09-08
            2026-09-09 2026-09-10 2026-09-11 2026-09-12 2026-09-13
          )

          for date in "${dates[@]}"; do
            for suffix in '' '-post' '-post-2'; do
              file="${date}${suffix}.jpg"
              url="https://fiestas.arandadeduero.es/daily-popular/${file}"
              output="dist/daily-popular/${file}"
              if curl --fail --silent --show-error --location --retry 3 --retry-delay 2 "$url" --output "$output"; then
                echo "Conservado $file desde Pages"
              else
                rm -f "$output"
                echo "No existe todavía $file en Pages"
              fi
            done

            file="${date}.json"
            url="https://fiestas.arandadeduero.es/daily-popular/${file}"
            output="dist/daily-popular/${file}"
            if curl --fail --silent --show-error --location --retry 3 --retry-delay 2 "$url" --output "$output"; then
              echo "Conservado $file desde Pages"
            else
              rm -f "$output"
              echo "No existe todavía $file en Pages"
            fi
          done

      - name: Configure Pages
        uses: actions/configure-pages@v6
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
